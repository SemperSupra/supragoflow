# Status - 2026-02-21

## Release Readiness Decision

- Recommended action now: **Pull Request (status/update only), not a new release**.
- Reason: release-hardening issue **#21** is still open:
  - https://github.com/SemperSupra/supragoflow/issues/21
- Current latest release remains `v0.0.1` (published 2026-02-18).

## Current Repository State

- Branch: `main`
- Sync: local and `origin/main` are aligned and clean.
- Open PRs: none at snapshot time.
- Open issues: 17 (defer/backlog-heavy set; #21 is release-critical).

## Completed This Session

- Merged docs refresh PR:
  - PR #49: https://github.com/SemperSupra/supragoflow/pull/49
- Implemented governance policy for container execution identity:
  - Issue #50 (closed): https://github.com/SemperSupra/supragoflow/issues/50
  - PR #51 (merged): https://github.com/SemperSupra/supragoflow/pull/51
- Enforced:
  - non-root default `USER` in both Dockerfiles
  - invoker UID:GID mapping in `scripts/gg` stage runs
  - policy conformance checks for execution identity rules
  - explicit documented root-only exception for short-lived volume ownership prep

## Validation Snapshot

Validated after policy changes:

- `./scripts/check-policy-conformance.sh` (pass)
- `./scripts/gg verify-writable` (pass)
- `./scripts/gg bootstrap` (pass)
- `./scripts/gg lint` (pass)
- `./scripts/gg test` (pass)

Earlier full readiness sweep (build/test/package/images/diagnose) passed except:

- `./scripts/gg smoke-windows` requires explicit `SUPRAGOFLOW_WINE_RUNNER_IMAGE` configuration in the executing environment.

## Remaining Release-Critical Item

1. **#21 Enforce release tag immutability safeguards in GHCR publish flow**
   - Add pre-publish check for existing tag/digest.
   - Fail on mismatch unless explicit override mode is used.
   - Emit actionable failure diagnostics.

## Next Steps

1. Implement and merge #21.
2. Re-run release readiness sweep including configured `SUPRAGOFLOW_WINE_RUNNER_IMAGE`.
3. If green, cut next release (`v0.0.2` or maintainer-selected semver bump).

## Session Resume Commands

```bash
cd /home/mark/Projects/SupraGoFlow/workspace
git pull --ff-only
gh issue view 21
./scripts/gg validate-config
./scripts/gg verify-writable
./scripts/gg test
./scripts/check-policy-conformance.sh
```
